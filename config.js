// config.js
const config = {
    theme: {
        background: "#111", // Dark background color
        textColor: "#fff",  // Main text color
        buttonColor: "#222", // Button background color
        linkColor: "#00aaff" // Button and link color
    }
};
